import styled from 'styled-components'

export const Container = styled.div`
  position: fixed;
  z-index: 2;
  bottom: 48px;
  left: calc(50%);
  transform: translateX(-50%); // Tira 50% do tamanho do elemento
`
